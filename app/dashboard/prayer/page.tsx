import PrayerManager from '@/components/prayer/PrayerManager';

export default function PrayerPage() {
  return (
    <div className="min-h-screen bg-gray-50">
      <PrayerManager />
    </div>
  );
}
